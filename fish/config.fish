set PATH /home/caio/bin/ /usr/lib/emscripten/ $PATH

set fish_greeting

alias dutotal 'du -cksh'
alias l 'ls'
alias ls 'ls --color -F'
alias la 'ls -la'
alias lat 'la -t'
alias ga 'git add'
alias gc 'git commit'
alias gd 'git diff'
alias gs 'git status'
alias ta 'tmux attach'
alias vim nvim
alias servidor 'mosh --ssh="ssh -p 26" caioalonso'
alias update 'sudo pacman -Syu'
